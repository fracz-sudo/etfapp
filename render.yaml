services:
  - type: web
    name: etf-screener
    env: node
    buildCommand: npx puppeteer browsers install chrome && npm install
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 20.x
